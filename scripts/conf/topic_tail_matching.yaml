defaults:
  - _self_
  - local: default

topics_path: ${data_path}/replication-data/annotated_github_topics_wikidata.json
candidates_path: ${data_path}/reconciled-gittopic.json
out_mapping: ${data_path}/topic_tail_mapping.csv