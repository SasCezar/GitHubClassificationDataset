defaults:
  - _self_
  - local: default

in_path: ${data_path}/raw_dataset.json # not required for replication as the out_path has same content
out_path: ${data_path}/repos_w_readme.json  # Integrates the data from the in_path file with the label_path and label_path_levels
label_path: ${data_path}/replication-data/annotated_github_topics_wikidata.json
label_levels_path: ${project_path}/notebooks/out/scatter.csv
tail_map_path: ${data_path}/topic_tail_mapping.csv
dataset_out: ${data_path}/classification_dataset.json  # Final dataset used for the classification